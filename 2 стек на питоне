# Стек на Python можно реализовать с помощью списка
stack = []

# Добавление элементов (append)
stack.append(10)
stack.append(20)
stack.append(30)

print("Верхний элемент:", stack[-1])  # 30

# Удаление верхнего элемента (pop)
print("Удалён:", stack.pop())         # 30
print("Теперь верхний:", stack[-1])   # 20

# Проверка на пустоту
print("Стек пуст?", len(stack) == 0)
